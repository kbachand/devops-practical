apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: devops-practical-claim0
  name: devops-practical-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
